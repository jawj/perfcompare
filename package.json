{
  "name": "perfcompare",
  "version": "0.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "serve": "serve -p 8008 --no-clipboard",
    "watch": "esbuild index.ts --bundle --keep-names --format=iife --target=es2022 --platform=neutral --minify --line-limit=80 --watch --outdir=.",
    "start": "npm-run-all --parallel --race watch serve"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.18.11",
    "npm-run-all": "^4.1.5",
    "serve": "^14.2.0"
  },
  "dependencies": {
    "@types/json-bigint": "^1.0.1",
    "@types/mithril": "^2.0.12",
    "json-bigint": "^1.0.0",
    "json-custom-numbers": "^2.0.16",
    "lossless-json": "^2.0.9",
    "mithril": "^2.2.2"
  }
}
