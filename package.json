{
  "name": "perfcompare",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "serve -p 8008 --no-clipboard",
    "watch": "esbuild index.ts --bundle --keep-names --format=iife --target=es2022 --platform=neutral --sourcemap=inline --watch --outdir=.",
    "start": "npm-run-all --parallel --race watch serve"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.17.19",
    "npm-run-all": "^4.1.5",
    "serve": "^14.2.0"
  },
  "dependencies": {
    "json-custom-numbers": "^2.0.7"
  }
}
